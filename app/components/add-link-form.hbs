<form {{on "submit" this.handleAdd}}>
  <div class="row">
    <div class="col-xs-12 col-sm-8 col-md-10">
      <div class="form-group">
        <Input
          @type="url"
          @value={{this.url}}
          data-test-url
          id="url"
          placeholder="URL to Add"
          class="form-control"
        />
      </div>
    </div>
    <div class="col-xs-12 col-sm-4 col-md-2">
      <div class="form-group">
        <BsButton
          @buttonType="submit"
          @type="primary"
          class="btn-block"
          disabled={{this.isSaving}}
          data-test-add-button
        >
          {{if this.isSaving "Adding…" "Add"}}
        </BsButton>
      </div>
    </div>
  </div>
  {{#if this.error}}
    <BsAlert @type="danger">
      {{this.error}}
    </BsAlert>
  {{/if}}
</form>
